export default function ProfileScreen({ adminHeader, profileForm, setProfileForm, profileError, profileSuccess, busy, onSubmit }) {
  return <main className="app-shell">{adminHeader}<section className="dashboard"><div className="section-heading"><div><h1>الملف الشخصي</h1></div></div><div className="dashboard-widget profile-card"><form onSubmit={onSubmit} className="login-form"><label>الاسم الكامل<input value={profileForm.fullName} onChange={(event) => setProfileForm({ ...profileForm, fullName: event.target.value })} required /></label><label>البريد الإلكتروني<input type="email" value={profileForm.email} onChange={(event) => setProfileForm({ ...profileForm, email: event.target.value })} required /></label><label>رقم الهاتف<input type="tel" value={profileForm.phone} onChange={(event) => setProfileForm({ ...profileForm, phone: event.target.value })} required /></label>{profileError && <p className="form-error" role="alert">{profileError}</p>}{profileSuccess && <p className="form-success" role="status">{profileSuccess}</p>}<button className="primary-button" type="submit" disabled={busy}>{busy ? 'جارٍ الحفظ…' : 'حفظ التغييرات'}</button></form></div></section></main>
}
