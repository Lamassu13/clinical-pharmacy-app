PORT=3002
DATABASE_URL=postgresql://clinical_pharmacy:change-me@localhost:5432/clinical_pharmacy_test
DATABASE_SSL=false
# Low cost keeps a suite of many logins fast; production still uses its own BCRYPT_COST.
BCRYPT_COST=4
